const config = {
  // REST API served by your app/backend
  apiBaseUrl: "http://127.0.0.1:4000/api",
  // PocketBase direct URL (bypass proxies for reliable SSE realtime)
  pbBaseUrl: "http://127.0.0.1:8090",
};

export default config;
